"use client";
/**
 * VersionCheck - Disabled for now to prevent potential reload issues
 * Can be re-enabled later when needed
 */
export default function VersionCheck() {
  // Temporarily disabled to prevent any potential issues
  return null;
}
